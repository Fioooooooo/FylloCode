## 1. Shared aggregate and identity contracts

- [x] 1.1 Add `src/shared/types/repository-browser.ts` with `RepositoryFolderStatus`, `RepositoryFolderResult<T>`, and `RepositoryAggregate<T>`; extend `src/shared/types/specs.ts` with `SpecRef`/`specRefKey`, `src/shared/types/guidelines.ts` with `GuidelineRef`/`guidelineRefKey`, and proposal/overview response types with per-Folder aggregate metadata. Update affected `src/shared/ipc/insight/**` and `src/shared/ipc/proposal/**` schemas plus shared schema tests so owner IDs are required and same local IDs in different Folders remain distinct.

## 2. Main repository aggregation

- [x] 2.1 Create `src/main/services/insight/repository-browser/aggregate.ts` to map every `ResolvedWorkspace.folders` member to stable `ready | missing | error` results, preserve item warnings, flatten only ready items, and compute `complete | partial` plus excluded Folder IDs. Add focused tests for ready-empty, missing, thrown leaf errors, item warnings, and member-order stability.
- [x] 2.2 Refactor `src/main/services/insight/specs/specs-browser-service.ts` and `src/main/ipc/insight/specs.ts` to resolve the full Workspace, scan each Folder through the aggregate helper, return owner-qualified `SpecRef` items, and distinguish missing directory from read/parse errors. Extend specs service and IPC tests for same-ID specs, partial failure, empty Folder, and sender Workspace scope.
- [x] 2.3 Refactor `src/main/services/insight/guidelines/guidelines-browser-service.ts` and `src/main/ipc/insight/guidelines.ts` to aggregate every Folder, reuse `scanGuidelines`, attach `GuidelineRef` and Folder metadata, and retain per-item frontmatter/read warnings. Extend focused tests for same-path guidelines, missing Folder, ready-empty directory, parse warning, and one-Folder I/O failure.
- [x] 2.4 Change `src/main/services/proposal/browser/proposal-service.ts`, proposal IPC/preload list contracts, and callers so proposal listing returns per-Folder aggregate state instead of silently converting Folder scan errors to `[]`; preserve `ProposalRef`, worktree metadata, stable ordering, and existing owner-qualified detail resolution. Update proposal service/IPC tests for partial scans and cross-Folder same-name changes.

## 3. Overview dual-scope aggregate

- [x] 3.1 Refactor `src/main/services/insight/overview/overview-service.ts`, `openspec-stats.ts`, `git-stats.ts`, and `archive-commit-index.ts` so `getProjectOverview(workspaceId)` resolves the Workspace once, reads Workspace subjects once, and runs per-Folder repository readers concurrently. Return owner-qualified active changes/evolution, count only ready Folders, expose completeness/excluded Folder IDs, avoid cross-Folder `changeId` dedupe, and never enrich from another Workspace subject. Update `src/main/ipc/insight/overview.ts` and focused tests for two Folders, partial failure, same-name proposals, one subject read, and passive lineage behavior.

## 4. Preload and renderer repository browsers

- [x] 4.1 Update `src/preload/api/{insight,proposal}.ts`, `src/preload/index.ts`, `src/preload/index.d.ts`, and renderer API wrappers to expose the new aggregate response types without accepting repository paths from renderer. Add/adjust preload tests for Specs, Guidelines, Overview, and Proposal list forwarding.
- [x] 4.2 Refactor `src/renderer/src/stores/insight/{specs,guidelines}.ts` and `src/renderer/src/pages/{specs,guidelines}.vue` to use complete Ref keys, retain per-Folder states, implement owner-preserving Folder filters, show owner badges and distinct ready-empty/missing/error/partial states, and reject previous-Workspace late responses. Update page/store tests for same-name selection, filter changes, partial data, accessibility text, and Workspace switch.
- [x] 4.3 Refactor `src/renderer/src/stores/proposal/browser.ts` and `src/renderer/src/pages/proposal.vue` for aggregate results and Folder filtering while preserving complete-list/no-status-tabs behavior, `ProposalRef` detail calls, worktree indicators, and current run/status watcher semantics. Update proposal renderer tests for missing/error Folder filters and same-name owner isolation.
- [x] 4.4 Update `src/renderer/src/stores/insight/overview.ts`, overview components, and `src/renderer/src/pages/overview.vue` to render owner-qualified active proposals, Folder-aware governance evolution, partial totals/excluded Folder warnings, and unchanged Workspace knowledge isolation/navigation. Extend overview tests for partial governance, same-name proposal detail owner, narrow layout, and late response rejection.

## 5. Workspace-owned automation

- [x] 5.1 Extend `src/shared/types/task.ts`, task IPC schemas, `src/main/infra/storage/task-store.ts`, task services/adapters, and renderer task store/page/modal so `targetFolderIds` are optional ordered-deduplicated soft references and reads expose `currentTargetFolderIds`/`staleTargetFolderIds` against current Workspace membership. Preserve legacy tasks without guessed targets, keep task targets out of member-removal blockers, show stale counts/edit controls, and add store/service/IPC/renderer tests.
- [x] 5.2 Add a pure owner-suggestion helper used by the task-to-chat/proposal flow in `src/renderer/src/stores/automation/task.ts`: preselect only when the original deduplicated target set has exactly one currently available Folder; preserve ambiguity when multiple original targets degrade to one, and validate external repository metadata against membership. Cover no-target, one-valid, one-stale, multi-target, and duplicate-target cases.
- [x] 5.3 Keep built-in workflows global and custom workflows under `workflowsDir(workspaceId)`; tighten `src/shared/ipc/automation/workflow.schemas.ts`, `src/main/ipc/automation/workflow.ts`, workflow service, preload/renderer wrappers and tests so Workspace-page list/save/delete mutations carry `workspaceId`, while existing proposal runs continue using their persisted ProposalRef/worktree target rather than current primary.
- [x] 5.4 Rename `project-integration` modules/channels/API/store symbols to `workspace-integration` across `src/shared/ipc/automation/**`, `src/main/{ipc,services,infra}/**`, preload, renderer API/store/page, and tests. Extend `WorkspaceIntegrationEntry` with optional `folderId`; validate new repository-bound source-control/CI entries against current Workspace membership, preserve legacy unbound and removed-member entries with current/stale projection, require explicit rebind on save, and keep Workspace-level resources unbound.

## 6. Scope settlement and documentation

- [x] 6.1 Audit local preview links, Fyllo Action handlers, plan events, spawned task sessions, Workflow, Task, Integration, Specs, Guidelines, Proposal, and Overview call sites with `rg` for `projectPath`, `projectId`, `resolveWorkspaceCwd`, primary fallback, and repository-derived app-data keys. Replace only remaining in-scope single-root assumptions with explicit `workspaceId`, Folder Ref, or `ResolvedWorkspace` inputs; add focused regression tests for each corrected call site and document any intentionally repository-local `*Path` parameters in `design.md`.
- [x] 6.2 Replace this proposal's temporary §23 inventory entries in `references/designs/multi-root-workspace/README.md` with links to `repository-browser-aggregation`, `specs-browser`, `guidelines-browser`, `proposal-browser`, `project-overview`, and `workspace-automation-storage`; update `guidelines/MainProcess.md` and `guidelines/RendererProcess.md` through the guideline maintenance workflow with aggregate reader, complete Ref, automation ownership, stale soft-reference, and Workspace Integration boundaries.

## 7. Verification

- [x] 7.1 Run affected shared/Main/Preload/Renderer focused Vitest, `pnpm typecheck`, `pnpm lint`, affected-file Prettier check, and `git diff --check`; fix failures and record results. Do not run full `pnpm build` and do not start `pnpm dev`.

  Validation: 47 focused test files / 342 tests passed across Main, Shared, Preload, and Renderer; 3 affected Main files / 41 tests passed again after import-boundary fixes. `pnpm typecheck`, `pnpm lint`, tracked/untracked affected-file Prettier checks, and `git diff --check` passed. Full `pnpm build` and `pnpm dev` were not run.
