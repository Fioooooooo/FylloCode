## 1. 持久化 contract 与 connection-local session 状态

- [x] 1.1 在 `src/main/domain/session/chat/acp-session-store.ts` 定义包含 `acpSessionId` 与 `configOptions` 的 recovery state，并把 `AcpSessionStore.loadAcpSessionId()` 替换为加载完整 state 的方法；更新 `src/main/infra/storage/chat-acp-session-store.ts` 复用 `loadSessionMeta()` 返回深度独立的配置快照，同时保持 `persistAcpSessionId()` 不覆盖现有 `configOptions`。在 `test/main/infra/storage/chat-acp-session-store.spec.ts` 覆盖有配置、无配置和更新 ACP ID 后配置仍保留。
- [x] 1.2 在 `src/main/infra/process/acp-process-pool.ts` 的 `AgentProcess` 增加 connection-local `activeSessionIds`，提供 mark/has/forget 窄 helper，并在 process dispose/invalidation 时自然清空；更新 `src/main/services/session/chat/session-probe-service.ts` 的 new/close/take/invalidation 路径维护 marker。扩展 `test/main/infra/process/acp-process-pool.spec.ts` 与 `test/main/services/session/chat/session-probe-service.spec.ts`，确认 marker 不跨 process generation 泄漏且 probe handoff 不错误关闭已提升 session。

## 2. 配置恢复规划与 ACP 编排

- [x] 2.1 新增 `src/main/domain/session/chat/session-config-recovery.ts`，实现基于 option ID/type/currentValue、flat/grouped select values 的纯恢复规划、incompatibility 分类与稳定 fingerprint；复用或下沉 `src/main/services/session/chat/acp-config-option-rpc.ts` 中的 `valueExistsInSchema()`，保持 domain 不依赖 ACP SDK。新增 `test/main/domain/session/chat/session-config-recovery.spec.ts`，覆盖一致值、removed/type-changed/invalid-value、boolean、grouped options、依赖 schema 变化与重复 snapshot。
- [x] 2.2 新增 `src/main/services/session/chat/session-config-recovery-service.ts`，使用 `buildPayload()`、`normalizeAcpSessionConfigOptions()` 和 `connection.setSessionConfigOption()` 串行执行 planner 产出的单项恢复；每个完整 response 重新规划，设置有限迭代/重复 fingerprint 防护，结构化记录不兼容 warning，并让合法候选的 method/transport/Agent error 失败整个恢复。新增 `test/main/services/session/chat/session-config-recovery-service.spec.ts`，证明 RPC 顺序、无冗余 set、缺失 lifecycle options 的强制确认、依赖重算、部分兼容降级及失败不返回未确认 snapshot。
- [x] 2.3 新增 `src/main/services/session/chat/acp-session-activation.ts`，单一实现 capability-aware `resumeSession → loadSession → optional newSession`、missing-session fallback、MCP/cwd 参数传递和 active marker 更新；允许 chat recovery 使用 fresh fallback，禁止 cold config mutation 创建 fresh session。新增 `test/main/services/session/chat/acp-session-activation.spec.ts`，覆盖全部 strategy、非 missing error 立即失败、config mutation 禁止 fresh 和旧 marker 切换。

## 3. 接入 Chat recovery 与配置修改

- [x] 3.1 修改 `src/main/services/session/chat/acp-session.ts`：从 `AcpSessionStore` 加载完整 recovery state，仅对当前 process 已 active 的 session 使用 direct prompt；cold session 通过 `acp-session-activation.ts` 激活，并在 resume/load/new 后调用 `session-config-recovery-service.ts`，只 emit 一次最终 `config_options_update`，再持久化 resolved ACP ID、注入既有 reminders 并发送 prompt。扩展 `test/main/services/session/chat/acp-session.spec.ts`，覆盖 restart resume/load/default response、fresh fallback、cold direct prompt 被跳过、warm direct prompt、missing/null options、恢复失败不 prompt及 reminder 只注入一次。
- [x] 3.2 修改 `src/main/services/session/chat/config-option-service.ts`：检测目标 ACP session 是否已在当前 process active；cold 时解析 bundled MCP servers，通过 `acp-session-activation.ts` 仅 resume/load，并先恢复 meta snapshot，再应用用户本次 option，最后一次性 patch Agent 确认的完整 snapshot。扩展 `test/main/services/session/chat/config-option-service.spec.ts`，覆盖 cold resume/load、无法激活时不 set/不 patch、已有 active session 不重复恢复及本次 value 使用恢复后的 live schema 校验。
- [x] 3.3 更新 `src/main/ipc/session/chat.ts` 的 control event 持久化测试，确保 raw lifecycle defaults、中间 set response 与失败 recovery 不会写入 session meta，只有 reconciliation 后的单个最终 `config_options_update` 会同步 renderer/meta；在 `test/main/ipc/session/chat.spec.ts` 覆盖 update 排序和 fresh ACP session ID 与最终配置不会互相覆盖。

## 4. 回归与质量门禁

- [x] 4.1 按 `guidelines/Testing.md` 运行聚焦 main tests：`pnpm exec vitest run --project main test/main/domain/session/chat/session-config-recovery.spec.ts test/main/services/session/chat/session-config-recovery-service.spec.ts test/main/services/session/chat/acp-session-activation.spec.ts test/main/services/session/chat/acp-session.spec.ts test/main/services/session/chat/config-option-service.spec.ts test/main/services/session/chat/session-probe-service.spec.ts test/main/infra/process/acp-process-pool.spec.ts test/main/infra/storage/chat-acp-session-store.spec.ts test/main/ipc/session/chat.spec.ts`，并修复所有失败。
- [x] 4.2 按 `guidelines/QualityGates.md` 完成 `pnpm typecheck:node`、`pnpm lint`、`pnpm test` 与 `git diff --check`；确认没有修改 session meta JSON shape、shared IPC 请求/stream event 或 renderer Config Options Bar contract。
