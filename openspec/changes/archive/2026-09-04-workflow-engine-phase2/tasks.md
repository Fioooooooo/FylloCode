## 1. 共享类型与 IPC 契约

- [x] 1.1 在 `src/shared/types/workflow.ts` 新增类型：`WorkflowProposalMode`（`"create"|"update"`）、`WorkflowProposalPersist`（`"session"|"workspace"`）、`WorkflowProposalStatus`（`"pending"|"confirming"|"confirmed"|"cancelled"`）、`WorkflowProposalSummary`、`WorkflowProposalDetail`（含 `yaml`、`mode`、`targetWorkflowId?`、`suggestedPersist`、`status`、`handoffDelivered?`）、`WorkflowProposalListRequest`、`WorkflowProposalDetailRequest`、`WorkflowProposalConfirmRequest`（含 `persist`）、`WorkflowProposalCancelRequest`、`WorkflowProposalWakePayload`。为 `WorkflowRunSnapshot`（同文件既有 `interface`）新增可选字段 `definitionSource?: "session" | "workspace"`。
- [x] 1.2 新建 `src/shared/ipc/automation/workflow-proposal.channels.ts`：定义 `AutomationWorkflowProposalChannels`（`list`/`getDetail`/`confirm`/`cancel`/`wake`），命名风格对齐 `src/shared/ipc/automation/workflow-run.channels.ts`。
- [x] 1.3 新建 `src/shared/ipc/automation/workflow-proposal.schemas.ts`：用 zod 定义对应输入 schema（`listWorkflowProposalsInputSchema`、`getWorkflowProposalDetailInputSchema`、`confirmWorkflowProposalInputSchema`、`cancelWorkflowProposalInputSchema`），风格对齐 `src/shared/ipc/automation/workflow-run.schemas.ts`。
- [x] 1.4 新建 `src/shared/ipc/automation/workflow-proposal-decision.channels.ts` + `.schemas.ts`：定义取消决定投递用的 `decisionList`/`decisionDispatch`/`decisionWake` channel 与 schema（owner 只需 `workspaceId`；`decisionDispatch` 额外需要 `notificationId`、`streamId`，形状对齐 `SessionChatNotificationChannels.dispatch` 但类型独立，不复用 `SpawnNotificationSummary`）。
- [x] 1.5 在 `src/mcp-servers/fyllo-workflow/src/rpc-schema.ts` 扩展 `workflowRpcMethodSchema` 为 `["list_workflows","trigger_workflow","describe_workflow_schema","propose_workflow"]`；新增 `describeWorkflowSchemaParamsSchema`（`{withExamples?: boolean}`）、`describeWorkflowSchemaResultSchema`（`{schema: string}`）、`proposeWorkflowParamsSchema`（`{yaml, persist, mode, workflowId?}`，`mode`/`workflowId` 用 discriminated union 表达"create 时禁止 workflowId、update 时必填"）、`proposeWorkflowResultSchema`（accepted/rejected 判别联合，`errors: Array<{rule,detail}>`）；在 `workflowRpcRequestSchema` 的 discriminated union 中加入这两个新 method 分支。

## 2. 持久化

- [x] 2.1 在 `src/main/infra/storage/workspace-paths.ts` 新增 helper：`workflowProposalsDir(workspaceId, parentSessionId)`、`workflowProposalDir(...)`、`workflowProposalDefinitionPath(...)`、`workflowProposalMetaPath(...)`、`workflowDecisionsDir(workspaceId, parentSessionId)`、`workflowDecisionPath(workspaceId, parentSessionId, proposalId)`、`sessionWorkflowsDir(workspaceId, sessionId)`、`sessionWorkflowDefinitionPath(workspaceId, sessionId, workflowId)`。全部复用现有 `assertStorageIdentity` 做路径安全校验。
- [x] 2.2 新建 `src/main/infra/storage/workflow-proposal-store.ts`：CRUD 函数 `saveWorkflowProposal`、`loadWorkflowProposal`、`listWorkflowProposals`、`updateWorkflowProposalStatus`，写入复用 `workflow-definition-store.ts` 里的 `withWriteQueue` + `writeFileAtomicSync` 模式（可直接复制该模式或抽出共享 helper，二选一，不强制抽象）。
- [x] 2.3 新建 `src/main/infra/storage/workflow-decision-store.ts`：定义 `WorkflowDecisionRecord` 落盘结构（`version`、`workspaceId`、`parentSessionId`、`proposalId`、`decision: "cancelled"`、`notification: {notificationId, state, updatedAt}`、`decidedAt`、`updatedAt`），提供 `saveWorkflowDecision`、`loadWorkflowDecision`、`listPendingWorkflowDecisions`、`claimWorkflowDecision`、`setWorkflowDecisionNotificationState`，方法签名对齐 `src/main/infra/storage/spawned-session-store.ts` 中对应的 spawn notification 函数。
- [x] 2.4 扩展 workflow definition 存储支持 session scope：在 `src/main/infra/storage/workflow-definition-store.ts` 新增 `saveSessionWorkflowDefinition(workspaceId, sessionId, workflowId, yaml)`、`loadSessionWorkflowDefinition(workspaceId, sessionId, workflowId)`（或新建独立 `session-workflow-definition-store.ts`，两种组织方式均可，选择后在 PR 描述中说明），复用 `workflowDefinitionPath`/`sessionWorkflowDefinitionPath` 对应的路径 helper 与既有原子写入队列模式。

## 3. `fyllo-workflow` MCP server

- [x] 3.1 新建 `src/mcp-servers/fyllo-workflow/src/schema-docs/schema.md` 与 `examples.md`：内容取自 `references/designs/workflow-engine/definition-schema.md` 第 1-9 节（骨架）与第 10 节（示例），作为包内维护的静态资产（设计文档 D1）。在 `package.json`/构建配置中确认这两个文件会被 `scripts/build-mcp-servers.mjs` 一起打进 `out/mcp-servers/fyllo-workflow/`（若当前构建脚本只拷贝 `.ts` 编译产物，需要额外加一步复制或改为 `import ... from "./schema-docs/schema.md?raw"` 风格内联进 bundle，参照 `src/mcp-servers/fyllo-specs/src/tools/instructions/create-proposal.md` 在同一 server 内的加载方式）。
- [x] 3.2 新建 `src/mcp-servers/fyllo-workflow/src/tools/describe-workflow-schema.ts`：注册 `describe_workflow_schema` tool，`inputSchema` 用 1.5 新增的 `describeWorkflowSchemaParamsSchema`；`withExamples: false` 只返回 `schema.md` 内容，`true` 时额外拼接 `examples.md`。description 需按设计文档第 3.1 节写"这是查询工具，不产生副作用"。结构对齐 `src/mcp-servers/fyllo-workflow/src/tools/list-workflows.ts`（同样通过 `rpc.call` 走 Main，即便内容是静态资产也统一走 RPC 分派，保持四个 tool 的调用路径一致）。
- [x] 3.3 新建 `src/mcp-servers/fyllo-workflow/src/tools/propose-workflow.ts`：注册 `propose_workflow` tool，description 按设计文档第 3.2 节写"call `describe_workflow_schema` first if you haven't already"。结构对齐 `trigger-workflow.ts`。
- [x] 3.4 在 `src/mcp-servers/fyllo-workflow/src/tools/index.ts` 注册新增的两个 tool；在 `src/mcp-servers/fyllo-workflow/src/rpc-client.ts` 确认新 method 能透传（若该文件是通用 RPC 调用封装，通常不需要改动，只需确认没有硬编码 method 白名单遗漏新 method）。

## 4. Main 自动化域与服务

- [x] 4.1 新建 `src/main/domain/automation/workflow/proposal-validator.ts`：纯函数 `validateProposeWorkflowInput({yaml, mode, workflowId, persist}, context)`，复用 `src/main/domain/automation/workflow/yaml-parser.ts` 的 `parseWorkflowYaml` 做 schema 解析期校验（第 9 节规则），并校验 `mode`/`workflowId`/`persist` 组合（`mode:"create"` 禁止 `workflowId`；`mode:"update"` 必须能解析到既有 session shadow 或 workspace definition）。返回 `{ok:true, definition}` 或 `{ok:false, errors: Array<{rule,detail}>}`，不做 execution-profile preflight（复用既有 `preflight.ts` 仅在 `trigger_workflow` 路径调用，这里不引入新依赖）。
- [x] 4.2 新建 `src/main/services/automation/workflow/workflow-proposal-service.ts`：实现 `proposeWorkflow`、`listProposals`、`getProposalDetail`、`confirmProposal`、`cancelProposal`。`proposeWorkflow` 调用 4.1 校验通过后用 `workflow-proposal-store.ts` 落盘并触发 wake（新增 `WorkflowProposalWakeDispatcher`，结构对齐 `src/main/ipc/automation/workflow-run.ts` 中的 `WorkflowRunWakeDispatcher`，debounce 逻辑可直接复制该类）。`confirmProposal` 实现设计文档 D4 的落盘矩阵（四个分支：create/update × session/workspace），成功后调用 4.4 的 handoff 逻辑；`cancelProposal` 写入 `workflow-decision-store.ts` 记录并标记 proposal `cancelled`。
- [x] 4.3 扩展 `src/main/services/automation/workflow/workflow-rpc-bridge.ts`：为 `describe_workflow_schema`/`propose_workflow` 两个 method 新增 handler 分支，路由到 4.2 的 `workflow-proposal-service.ts`；保留 `list_workflows`/`trigger_workflow` 现有到 `workflow-engine.ts`/`workflow-service.ts` 的路由不变。`callerType !== "chat"` 时对 `propose_workflow` 复用既有 `WORKFLOW_INVALID_CALLER` 拒绝分支；`describe_workflow_schema` 不做 caller 类型限制。
- [x] 4.4 新建 `src/main/services/automation/workflow/workflow-decision-service.ts`：方法形状对齐 `src/main/services/session/spawn/spawn-notification-service.ts`（`list`/`claim`/`buildReminder`/`markDelivered`/`markDeliveryUnknown`/`suppress`/`reconcileWorkspace`/`setWakeHandler`），读写 4.3 节新建的 `workflow-decision-store.ts`，`buildReminder` 生成不可见 `<system-reminder>` 文案说明"某个 workflow 提案已被取消"。
- [x] 4.5 在 `src/main/services/session/chat/chat-turn-service.ts` 新增 `executeWorkflowDecisionTurn`（结构对齐既有 `executeNotificationTurn`，改为读取 4.4 的 `workflowDecisionService.buildReminder`）与 `claimWorkflowDecisionTurn`（对齐 `claimSpawnNotificationTurn`，内部用 `chatTurnGate.tryAcquire(workspaceId, parentSessionId, "notification")`，不新增 `ChatTurnKind`）。同文件内新增 `sendWorkflowConfirmHandoff(record)`：confirm 成功后调用，尝试同一 `chatTurnGate` lease，拿到则立即以 `role:user` 追加 reminder 并驱动一次 turn（不经过 `workflow-decision-store.ts`，也不建立五状态记录，只写回 proposal metadata 的 `handoffDelivered` 布尔值，见设计文档 D5），拿不到则直接放弃，不重试。
- [x] 4.6 扩展 `src/main/services/automation/workflow/workflow-engine.ts` 的 Run 创建路径（`trigger_workflow` 对应实现）：定义解析改为先查 `sessionWorkflowDefinitionPath` 再查 `workflowDefinitionPath`，命中来源写入 snapshot 的新字段 `definitionSource`；两处都未命中时沿用既有 `WORKFLOW_NOT_FOUND` 错误路径。

## 5. Renderer 侧 IPC 三层

- [x] 5.1 新建 `src/main/ipc/automation/workflow-proposal.ts`：`registerWorkflowProposalHandlers()`，`list`/`getDetail`/`confirm`/`cancel` 均先 `requireWorkspaceSender` 再 `assertSessionBelongsToWorkspace`（复用 `src/main/ipc/automation/workflow-run.ts` 中 `assertWorkflowRunOwner` 的实现方式），业务逻辑委托给 4.2 的 `workflow-proposal-service.ts`。新增 `WorkflowProposalWakeDispatcher`/`setupWorkflowProposalBroadcast`，与 `setupWorkflowRunBroadcast` 对称，在 `src/main/bootstrap/runtime.ts` 里同 Run broadcast 一起注册。
- [x] 5.2 新建 `src/main/ipc/automation/workflow-proposal-decision.ts`：注册 4.4/4.5 节对应的 `decisionList`/`decisionDispatch` handler（结构对齐 `src/main/ipc/session/chat.ts` 里 `SessionChatNotificationChannels.list`/`dispatch` 的注册方式，但改用 1.4 新建的独立 channel/schema），以及 `decisionWake` 广播（`workflowDecisionService.setWakeHandler` 挂到 `WorkspaceWindowManager.sendToWorkspace`，比照 `setupSpawnNotificationBroadcast`）。
- [x] 5.3 新建 `src/preload/api/automation/workflow-proposal.ts`：暴露 `window.api.automation.workflowProposal.{list,getDetail,confirm,cancel,onWake}`，风格对齐 `src/preload/api/automation/workflow-run.ts`。同样为 decision 相关 channel 加 `decisionList`/`decisionDispatch`/`onDecisionWake`（可并入同一文件或独立 `workflow-proposal-decision.ts`，与 Main 侧文件拆分保持一致）。

## 6. System reminder 能力提示

- [x] 6.1 新建 `src/main/services/session/chat/system-reminder/providers/workflow.ts`：导出 `resolveWorkflowCapabilitySection()`，返回设计文档第 4.1 节给出的固定文案（"当用户明确要求把当前流程保存或复用为 workflow 时，先调用 `describe_workflow_schema` 了解格式，再用 `propose_workflow` 提交生成结果供用户确认..."）。
- [x] 6.2 在 `src/main/services/session/chat/system-reminder/providers/chat.ts` 的 `resolveChatSystemReminder` 中，`knowledgeSection` 之后、`renderFylloActionPromptContract()` 之前插入 6.1 的返回值（加入 join 数组对应位置）。确认 `resolveSystemReminder`（`system-reminder/index.ts`）对 `ctx.owner === "workflow"` 仍然直接返回 `null` 的既有分支不受影响（fresh Agent stage 不应看到这段提示，不需要改动该文件）。

## 7. Renderer feature 与 store

- [x] 7.1 新建 `src/renderer/src/api/automation/workflow-proposal.ts`：包装 5.3 的 preload API，风格对齐 `src/renderer/src/api/automation/workflow-run.ts`。
- [x] 7.2 新建 `src/renderer/src/stores/automation/workflow-proposal.ts`：Pinia store，接口对齐 `src/renderer/src/stores/automation/workflow-run.ts`（`acquireListInterest`/`acquireDetailInterest`/`loadDetail`/`handleWake`/`confirm`/`cancel`/`detailState`），按 `workspaceId+parentSessionId+proposalId` 隔离 state，晚到 response 按 generation 丢弃。
- [x] 7.3 新建 `src/renderer/src/features/workflow-proposal-review/model/projection.ts`：`projectWorkflowProposalDetail`、`workflowProposalStatusPresentation`、`isProposalActionableStage`（用于第 8.3 节 `confirm` 非 `true` 的高亮判断）等纯函数，对齐 `src/renderer/src/features/workflow-run-inspector/model/projection.ts` 的组织方式。
- [x] 7.4 新建 `src/renderer/src/features/workflow-proposal-review/application/use-workflow-proposal-review.ts`：`useWorkflowProposalReview(target)`（对齐 `useWorkflowRunInspector`：`open/detail/refresh/openDetail/closeDetail/confirm/cancel`）与 `useWorkflowProposalListInterest(input)`（对齐 `useWorkflowRunListInterest`）。
- [x] 7.5 新建 `src/renderer/src/features/workflow-proposal-review/integration/wake.ts`：`registerWorkflowProposalWakeListener(pinia)`，实现对齐 `src/renderer/src/features/workflow-run-inspector/integration/wake.ts`。
- [x] 7.6 新建 `src/renderer/src/features/workflow-proposal-review/ui/WorkflowProposalActivityEntry.vue` 与 `ui/WorkflowProposalConfirmCard.vue`：用 @nuxt/ui v4 的 `UTimeline`（`orientation: "vertical"`）搭建竖向时间线骨架（设计文档第 8.6 节），底部固定"取消 / 仅本次执行 / 保存为可复用并执行"操作栏；对 `exec`/`webhook` 且 `confirm` 非 `true` 的 stage 加醒目角标；`mode:"update"` 时在卡头展示目标 workflow 名称与 `workflowId`。
- [x] 7.7 新建 `src/renderer/src/features/workflow-proposal-review/index.ts`：导出 7.3-7.6 的公共入口，对齐 `workflow-run-inspector/index.ts` 的导出粒度；补一份 `README.md` 说明本 feature 范围（比照 `workflow-run-inspector/README.md`）。
- [x] 7.8 修改 `src/renderer/src/components/chat/ChatBackgroundActivityBar.vue`：在既有 `<WorkflowRunActivityEntry>` 旁新增 `<WorkflowProposalActivityEntry :workspace-id="activeSession.workspaceId" :parent-session-id="activeSession.id" />`，从 `@renderer/features/workflow-proposal-review` 导入。
- [x] 7.9 注册 7.5 的 wake listener：在应用 renderer 启动装配点（`registerWorkflowRunWakeListener` 当前被调用的位置，通常在 `src/renderer/src/main.ts` 或 store/plugin 初始化处）同步调用 `registerWorkflowProposalWakeListener`。

## 8. Guideline 更新

- [x] 8.1 更新 `guidelines/MainProcess.md` 第 76 条（"MUST 让 completion notification 采用 Main 生成、持久化、at-most-once 的 owner-scoped outbox..."）：补充说明该 owner-scoped outbox + `ChatTurnGate` 模式现在有两个独立实例——spawn completion notification（`spawn-notification-service.ts`）与 workflow proposal 取消提醒（`workflow-decision-service.ts`）——两者共用 `ChatTurnGate`/`chat-turn-service.ts` 的 turn 互斥与投递骨架，但存储、类型和 IPC channel 保持独立，不合并成单一协议；补充证据文件 `src/main/services/automation/workflow/workflow-decision-service.ts`、`src/main/infra/storage/workflow-decision-store.ts`。

## 9. 测试

- [x] 9.1 `test/main/domain/automation/workflow/proposal-validator.spec.ts`：覆盖 schema 合法/非法、`mode`/`workflowId`/`persist` 组合校验的全部分支，含"schema 合法但超出 execution profile 仍通过"的用例。
- [x] 9.2 `test/main/infra/storage/workflow-proposal-store.spec.ts` 与 `workflow-decision-store.spec.ts`：覆盖并发写入队列、原子写入、跨 owner 隔离。
- [x] 9.3 `test/main/services/automation/workflow/workflow-proposal-service.spec.ts`：覆盖 confirm 落盘矩阵四个分支（含"session shadow 升格 workspace 但目标不存在"的拒绝路径）、重复 confirm/cancel 只返回既有终态、confirm handoff 的"空闲立即投递"与"忙碌放弃"两条路径。
- [x] 9.4 `test/main/services/automation/workflow/workflow-decision-service.spec.ts`：覆盖 pending/dispatched/delivered/delivery_unknown/suppressed 五状态转换，特别是重启把 dispatched 转 delivery_unknown、parent 删除把非终态转 suppressed。
- [x] 9.5 `test/main/services/automation/workflow/workflow-engine.spec.ts` 补充：session shadow 优先于 workspace definition 的解析顺序、`definitionSource` 写入 snapshot、session shadow 删除后历史 Run 不受影响。
- [x] 9.6 `test/main/ipc/automation/workflow-proposal.spec.ts`、`workflow-proposal-decision.spec.ts`：覆盖 owner 校验、跨 session/workspace 拒绝、wake payload 不携带 YAML。
- [x] 9.7 `test/mcp-servers/fyllo-workflow/describe-workflow-schema.spec.ts`、`propose-workflow.spec.ts`：覆盖 RPC 请求/响应 schema、非 chat caller 拒绝、`withExamples` 开关。
- [x] 9.8 `test/renderer/src/stores/automation/workflow-proposal.spec.ts`：覆盖 interest-gated pull、wake 触发拉取、晚到 response 丢弃。
- [x] 9.9 `test/renderer/src/features/workflow-proposal-review/**`：覆盖确认卡片完整展示 YAML（不折叠）、`confirm` 非 `true` 的醒目标记、`mode:update` 标注目标 workflow、操作栏固定底部。
- [x] 9.10 `test/renderer/src/components/chat-background-activity-bar.spec.ts` 补充：Proposal 与 Run entry 同时展示、互不影响彼此数量/状态。
