<!--
模板：复制到 <review-name>/open/I<n>.md。
上限 300 行。往返记录只在末尾追加，不改历史发言。
收敛后：结论写入 decisions.md → git mv 到 archive/ → 更新 MAILBOX.md。
-->

# I&lt;n&gt; · &lt;一句话标题&gt;

**级别**：阻塞 / 高 / 中 / 低 | **提出**：R&lt;n&gt; · &lt;reviewer|author&gt; | **待办方**：&lt;author|reviewer|human&gt;

## 问题陈述

<不随轮次变化；后续修正在往返记录中体现，不改写本节。>

**证据**：

- `path/to/file.ts:12-20` —— <断言>
- 已在事实表中的用 `F<n>` 引用，不重复验证。

**范围说明**：<为什么它在 contract.md 声明的范围内。>

## 往返记录

**[R&lt;n&gt;] reviewer：** <要求，逐条编号。>

**[R&lt;n+1&gt;] author：** <接受 / 修订 / 反驳，逐条对应。>

**修订声明**（author 每次修订必填）：

- 新增/变更字段：<无 | 列出>
- 新增不变量：<无 | 列出>
- 新增用户承诺：<无 | 列出，尤其是不可逆操作的措辞>
- 落点：`<产物>:<行号区间>`

**[R&lt;n+2&gt;] reviewer：** <复核结果。改变立场时写明「撤回 / 收回 / 维持」。>

## 共识结论

_待复核。_

<!--
收敛时替换为：
**已收敛（R<n>）。** 结论见 `decisions.md` 的 I<n> 小节。
遗留给下游的中/低级别约束：<无 | 列出并确认已写入实施约束清单>
-->
