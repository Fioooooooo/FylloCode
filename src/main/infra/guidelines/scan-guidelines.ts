// Re-exported from fyllo-cortex so the reminder-injected guidelines index and the
// MCP `guidelines` tool's state scan stay backed by the same implementation.
export { scanGuidelines } from "../../../mcp-servers/fyllo-cortex/src/utils/scan-guidelines";
export type { GuidelineEntry } from "../../../mcp-servers/fyllo-cortex/src/types/guideline";
