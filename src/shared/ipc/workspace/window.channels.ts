export const WorkspaceWindowChannels = {
  getContext: "workspace:window:getContext",
  openWorkspace: "workspace:window:openWorkspace",
  openFolder: "workspace:window:openFolder",
  openLauncher: "workspace:window:openLauncher",
} as const;
